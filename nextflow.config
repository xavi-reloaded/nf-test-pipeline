/*
 * Configuración básica de Nextflow
 */

process {
    executor = 'slurm'
    queue = 'debug'  // ajusta según tu cluster
    cpus = 1
    memory = '1 GB'
    time = '5m'
}

docker.enabled = false

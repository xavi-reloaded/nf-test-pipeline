process {
    executor = 'slurm'
    queue = 'debug'
    cpus = 1
    memory = '1 GB'
    time = '5m'
}

tower {
    enabled = true
    accessToken = "prod-secret"
    endpoint = "http://localhost:3000/"
    workspaceId = "1"
}

docker.enabled = false
